<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="https://fonts.googleapis.com/css?family=Roboto">
    <link rel="stylesheet" href="/assets/css/agent/collection.css">
</head>
<body>
    <main>
        <%- include('../partials/agentSidebar') %>
        <div id="main-wrapper">
            <div class="page-header">
                <span class="me-3" id="sidebar-toggler-btn"><i class="fas fa-bars"></i></span>
                <h5 class="m-0">Collection</h5>
            </div>

            <div class="collection-details">
                <span>Donor Name: <%= collection.donor.firstName + " " + collection.donor.lastName %></span>
                <span>Food type: <%= collection.foodType %></span>
                <span>Quantity: <%= collection.quantity %></span>
                <span>Time of cooking: <%= collection.cookingTime.toLocaleString("en-IN", { dateStyle: "medium", timeStyle: "short"}) %></span>
                <span>Address to collect: <%= collection.address %></span>
                <span>Phone: <%= collection.phone %></span>
                <span>Status: <span class="fw-bold text-<%= collection.status %>"><%= collection.status %></span></span>

                <% if(collection.status == "assigned" && collection.adminToAgentMsg != "") { %>
                    <span>Message from Admin:</span>
                    <div class="ms-3"><%= collection.adminToAgentMsg %></div>
                <% } %>

                <% if(collection.status == "collected") { %>
                    <span>Collection time: <%= collection.collectionTime.toLocaleString("en-IN", { dateStyle: "medium", timeStyle: "short"}) %></span>
                <% } %>
            </div>
        </div>
    </main>
</body>
</html>
